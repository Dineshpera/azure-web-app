name: Hello from DK 👋

on:
  push:
    branches:
      - main
  workflow_dispatch:  # allows manual trigger too

jobs:
  say_hello:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Say Hello
        run: echo "👋 Hello from Dinesh Karthik! Your workflow is running successfully."
